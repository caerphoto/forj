<div id="static_wrapper">
<h1>Sign up</h1>
<p>This is all we need to know:</p>

<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <p><%= f.label :name %>:<br />
  <%= f.text_field :name, :class => "signtextbox" %><br />
  <em>This is the name that will appear on your posts.</em></p>

  <p><%= f.label "E-mail" %>:<br />
  <%= f.text_field :email, :class => "signtextbox" %><br />
  <em>This is what you will use to sign in. Only forum administrators can see this &ndash; it is not shown to normal users.</em></p>

  <p><%= f.label :password %>:<br />
  <%= f.password_field :password, :class => "signtextbox" %><br />
  <em>Maximum length is 20 characters. You can use any characters you like.</em></p>

  <p><%= f.label "Confirm password" %>:<br />
  <%= f.password_field :password_confirmation, :class => "signtextbox" %></p>

  <p><%= f.submit "Sign me up!" %></p>
<% end %>

</div>
